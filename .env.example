# Optional: direct backend URL (no trailing slash, no /api path).
# On Vercel: leave unset to use /api proxy via vercel.json (recommended).
# Local dev: defaults to http://localhost:4000
VITE_API_URL=http://localhost:4000
